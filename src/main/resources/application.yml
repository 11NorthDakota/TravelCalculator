myapp:
    logDir: "C:/Logs"

spring:

  liquibase:
    enabled: true

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: Artem2003
    driver-class-name: org.postgresql.Driver

  jpa:
    database: postgres
    show-sql: true
    hibernate:
      ddl-auto: validate