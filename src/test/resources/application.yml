spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: artei
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
  jpa:
    database: h2
    show-sql: true
    hibernate:
      ddl-auto: validate

  liquibase:
    enabled: false
